# =============================================================================
# Credibility AI - Environment Configuration
# =============================================================================
# Copy this file to .env in the project root
# cp deploy/env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# AWS Configuration (for S3, Textract, and Bedrock)
# -----------------------------------------------------------------------------
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# -----------------------------------------------------------------------------
# AWS Bedrock Configuration (Claude with Vision support)
# -----------------------------------------------------------------------------
# Bedrock region - use us-east-1 for best model availability
BEDROCK_REGION=us-east-1
# Claude 3.5 Sonnet v2 (supports vision/images)
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0
# Alternative: Claude 3 Haiku (cheaper, also has vision)
# BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0
# WARNING: Claude 3.5 Haiku does NOT support vision - only text!

# -----------------------------------------------------------------------------
# Database Configuration (Local MySQL on same EC2)
# -----------------------------------------------------------------------------
# DB_HOST is set to host.docker.internal in docker-compose.yml
# This allows container to connect to MySQL running on EC2 host
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=credibility_db

# -----------------------------------------------------------------------------
# Processing Configuration
# -----------------------------------------------------------------------------
# OCR Mode: 'ocr+llm' (Textract + Claude) or 'llm' (Vision API only)
OCR_MODE=ocr+llm

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
